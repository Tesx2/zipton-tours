[build]
  # This runs the build script we defined in package.json
  command = "npm run build"
  # This tells Netlify to serve the files in your root directory
  publish = "."

[functions]
  directory = "netlify/functions"